# PulseAgent — API-only dependencies for Render deployment
# Full dependencies (evaluation, embeddings, data) are in requirements.txt

# Core pipeline
langgraph>=0.2.0
langchain-core>=0.2.0
pydantic>=2.7.0
httpx>=0.27.0
pyyaml>=6.0.0

# LLM
groq>=0.9.0

# Environment
python-dotenv>=1.0.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Data (lightweight — loaders used at runtime only if datasets present)
pandas>=2.2.0

# Terminal output
rich>=13.7.0
